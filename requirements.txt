# Web framework (FastAPI server kept for local dev / API testing)
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Frontend
streamlit==1.41.1

# Data processing
pandas==2.2.3
numpy==2.0.2
openpyxl==3.1.5
pyarrow==18.1.0

# Validation & configuration
pydantic==2.10.3
pydantic-settings==2.6.1

# Database (optional — app runs without DATABASE_URL)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# Visualization
matplotlib==3.9.3
plotly==5.24.1
# kaleido removed — not used (matplotlib is the rendering backend) and
# kaleido 0.2.1 has no Python 3.12 pre-built wheel.

# AI
anthropic==0.40.0

# Utilities
python-dotenv==1.0.1
# httpx pinned: anthropic requires httpx>=0.23,<1
httpx==0.27.2
